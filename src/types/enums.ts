export enum UserRole {
  SUPERADMIN = "superadmin",
  MANAGER = "manager",
  USER = "user",
}

export enum ProductStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
  SOLD = "sold",
  EXPIRED = "expired",
}